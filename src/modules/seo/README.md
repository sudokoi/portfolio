# Discovery

Owns shared metadata, safely serialized JSON-LD and the branded `ImageResponse` template. Routes for RSS, robots and sitemap remain thin and use the content module. OG uses bundled font/logo bytes, not remote fonts or Chromium screenshots. This is a server-only module surface.
