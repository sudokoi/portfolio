# Journal

Owns `/blogs` and `/blog/[slug]`, server-rendered Portable Text, build-time highlighting, and the CSS halo presets. Pages and their CSS modules are co-located. Only the on-demand search UI is a client component; all articles are readable without JavaScript.
